import React from 'react';
import Dashboard from '../../../components/Dashboard';

const Billing = (): JSX.Element => {
    return (
        <div>
            <Dashboard initialSelectedIndex={1} initialSelectedGroup={3} />
        </div>
    );
};

export default Billing;
